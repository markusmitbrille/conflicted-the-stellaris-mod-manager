using Conflicted.Model;

namespace Conflicted.Grammar
{
    class KeyedElementListener : ElementListener
    {
        public KeyedElementListener(ModFile file) : base(file)
        {
        }
    }
}